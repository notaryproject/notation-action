{
  "name": "notation-setup",
  "version": "1.0.0",
  "description": "Install and configure the Notation CLI and plugins.",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map --minify --license licenses.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/notaryproject/notation-action.git"
  },
  "keywords": [],
  "author": "duffney",
  "license": "Apache-2.0",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@actions/tool-cache": "^2.0.1",
    "mv": "^2.1.1",
    "semver": "^7.5.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
